# Timed IQ Assessment Application

A clean, responsive visual interface containing a 20-question logical evaluation bank, explicit countdown tracking constraints, and standard normalization scaling that structures a performance calculation output.

## Local Git Initialization
Open your terminal environment inside this directory to convert this into a tracking repository:

```bash
git init
git add index.html style.css script.js README.md
git commit -m "Initial commit: full structural setup for timed testing UI"